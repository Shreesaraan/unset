def unset(number,bit):
    n=(1<<bit)
    if number&n:
        number=number&~n
    return number 

number=int(input("Enter the Number : "))
bit=int(input("Enter the Bit to Unset : "))
print(unset(number,bit))